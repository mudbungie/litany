# Repo-wide tarpaulin defaults: applied by every `cargo tarpaulin` run,
# including the pre-commit hook.
#
# Pinned tarpaulin version: 0.35.2 (also pinned in .github/workflows/ci.yml).
# 0.35.3 was yanked, and 0.35.4+ silently excludes inline `#[cfg(test)] mod
# tests;` files from the coverable denominator. The 100% floor only means
# the same thing on every machine if everyone is on the same tarpaulin —
# upgrade in lockstep (workflow + this file + Makefile preflight) or the
# floor gets quietly weaker on whichever side moves first.
#
# - Schema generation (`config::schemas`) is unit-tested directly and
#   pinned by the `schemas_golden` in-crate golden test (which replaced
#   the former `gen-schemas` binary, ARCH §3.4): `write_to` runs on every
#   test pass (into a throwaway dir), so no generation mechanism sits
#   outside the coverage floor.
# - `ignore-panics` excludes branches that only fire on programmer-error
#   panics (e.g. `.expect(...)` on infallible operations).
# - `src/e2e/*` is the end-to-end subprocess-test boundary, the coverage
#   analogue of the `src/bin/*` exec-binding boundary above. These modules
#   drive the real `litany` binary as a subprocess and carry failure-only
#   scaffolding (timeout kills, stderr drains, early-exit diagnostics) that
#   a passing run never executes. They were migrated in from `tests/`
#   (which tarpaulin never counts in the denominator) only because the
#   §3.4 narrowing made their private fixtures unreachable from an external
#   test crate — so excluding them keeps the exact denominator they had as
#   `tests/`. The production source they drive stays in the denominator —
#   but see the next bullet: what a spawned binary executes never lands in
#   the numerator, so every counted line needs an in-process test.
# - Subprocess profraws are inert (measured, bl-7e33). Tarpaulin sets
#   `LLVM_PROFILE_FILE=<target>/tarpaulin/profraws/<test-binary>_%m-%p.profraw`,
#   every spawned `litany` inherits it, and its profraw lands in the same
#   pool the test binary's does; tarpaulin then merges the whole pool
#   *by function name* and maps the result through the test binary's
#   coverage map alone. Those foreign records cannot corrupt ours: the
#   lib compiled `--cfg test` and the lib linked into the `litany` bin
#   carry different crate disambiguators, so no repo function shares a
#   name across the two images (e.g. `prompt::budget::check` is
#   `_RNvNtNtCs5GBNLOVA7gO_6litany6prompt6budget5check` in one and
#   `_RNvNtNtCslo6p4TyE50k_…` in the other). They also cannot help: a
#   record no name in the coverage map matches is simply dropped. Hence
#   the rule above — and hence there is nothing to gain by scrubbing
#   `LLVM_PROFILE_FILE` at the spawn seam.
# - `timeout` is bumped above tarpaulin's 60s default because the
#   `src/e2e/prompt_end_to_end.rs` test chains two subprocess binaries
#   (`litany new` + `litany prompt`) under instrumentation, which inflates
#   wall-clock time well past the per-test default.
# - `engine = "Llvm"` is load-bearing and THIS FILE is its only home
#   (bl-6f1b). A `--engine llvm` on the command line is silently
#   discarded whenever this file exists: tarpaulin builds its configs
#   from the file and `Config::merge` never carries `engine` over from
#   the CLI, so the engine falls back to the platform default — ptrace
#   on Linux. Under ptrace, every `Command::spawn` in the bash-tool
#   tests vforks a shell child that tarpaulin auto-attaches
#   (PTRACE_O_TRACEVFORK) but never registers (its PTRACE_EVENT_VFORK
#   handler skips the pid map when follow-exec is off), so a breakpoint
#   trap in that child goes unserviced and the run dies with `Failed to
#   find process for pid: …` → `A segfault occurred while executing
#   tests`. The race is timing-sensitive: it fired reliably on warm
#   2-core CI runners and essentially never on a 16-core box. The llvm
#   engine spawns tests as plain children — no ptrace, no race — and
#   bl-7e33 measured its profraw merge inert (bullet above), so the
#   100% floor means the same thing under it.
# - `args = ["--test-threads=1"]` pins serial test execution: the
#   bash-tool cascade tests deliver SIGTERM/SIGKILL to spawned shell
#   process groups, and serial teardown keeps those groups from
#   interleaving. (The llvm engine would force one test thread anyway
#   — rust#91092 — this line just says so out loud.)
[default]
engine = "Llvm"
exclude-files = ["src/bin/*", "src/bin/litany/*", "src/e2e/*", "crates/*/src/main.rs"]
ignore-panics = true
timeout = "300s"
args = ["--test-threads=1"]
