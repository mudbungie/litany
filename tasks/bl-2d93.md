+++
title = "compactors die on the gpt route with 'No tool call found for function call output with call_id …': a compaction or history cut leaves a tool result whose call is gone, and the Responses API refuses the replay"
created = 1788753120
updated = 1788753176
claimant = "Cantaloups-L10"
priority = 1
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
tags = ["usability-r3"]
+++
Seen on the operator's server workspace 2026-09-06 (phone screenshot): three compactor conversations (OvalChisel, BellowsPaprika, BroomCedar) dead with the provider error 'No tool call found for function call output with call_id call_…'. That is the OpenAI Responses API refusing a history that carries a function_call_output whose function_call is absent. Likely causes on today's tree: bl-2071's landing sweep deletes every messages/** entry the summary replaces and may cut inside a tool window (call in a deleted entry, result kept, or the reverse); or the cut at max_tokens/OutputTruncated leaving a dangling pair; or the memory 'interrupted tool window wedge' shape (a killed step). Rule to enforce structurally: a tool call and its result are one unit for every cut — compaction sweep, prune, truncation, interrupt — never split; and the assembler refuses to send an orphan (drops it with a recorded note) so a wedged history revives instead of dying at every prompt. Reproduce on a two-step gpt fixture and on the claude row (Anthropic 400s the same shape as 'tool_result without tool_use').