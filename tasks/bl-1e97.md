+++
title = "promote the accumulated [Unreleased] section as 0.0.4, ahead of the release PR merge"
created = 1788152635
updated = 1788152635
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
The release PR queued on main proposes 0.0.3 -> 0.0.4 and touches manifests only — `changelog_update = false` in `release-plz.toml`, so release-plz never writes CHANGELOG.md and the hand-kept `[Unreleased]` section is the only complete record of what this version ships.

Per the release-plz.yml header: land `make promote-changelog VERSION=0.0.4` on main BEFORE merging the release PR, so the tagged tree carries the stamped section rather than an unreleased one.

0.0.4 carries the injection-seam amendment (bl-ddaa: `tools()` asked per named agent, `RoutedCall::cwd` carrying the caller resolved working directory), both compaction fixes (bl-b9f0 in-flight suppression of the next checkpoint, bl-c7bb a pass may not shed its own summary), and the disclosure-gate session-artifact URL form (bl-1408).

Deliverable: the promoted CHANGELOG.md heading on main. No manifest edit here — that is the PR job.