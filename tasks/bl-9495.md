+++
title = "driver operator-notice stderr lines need a stable machine-readable prefix"
created = 1787379502
updated = 1788138514
claimant = "OrderTinker"
priority = 2
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Consumer report (yog): the ARCH §'stderr is different in kind' contract routes operator notices (compaction landing declined/superseded, budget stops, accepted-crash notes, retarget declines — child_result.rs, advance.rs, advance/crash.rs, terminal.rs, driver.rs) onto the driver's stderr. A consumer that captures a detached driver's stderr (yog's per-spawn sink) cannot distinguish these Ok-path notices from a death rattle except by phrase-matching prose, which is brittle across releases. Ask: a stable prefix on every operator-notice line (e.g. 'lernie: notice: ...'), or alternatively route the in-process prompt loop's notices to steps/<agent>/driver.log like launched drivers (ARCH notes the fresh-root prompt loop as an open simplification). No exit-code change wanted — the codes are correct. yog ships a narrow phrase classifier meanwhile and will key on the prefix when it exists.