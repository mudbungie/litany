+++
title = "role config gains priority: the assignment asks the provider's priority lane through the adapter's service-tier knob"
created = 1788321297
updated = 1788321297
priority = 2
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
**GATED on brazen bl-fbb0 (that repo's board): the lifted `service_tier` canonical knob, published.** Until a brazen release carries `CanonicalRequest.service_tier` and the `Cargo.toml` pin (`brazen = "="` — the pin's one home) is bumped to it, this field has nowhere to go; do not shim it through `extra` (the typed-request discipline makes `extra` unreachable on purpose, ARCH §4.4).

**Shape — the exact mirror of `effort:` (bl-acba, landed).**
- `RoleAssignment` (`src/config/per_repo_providers.rs`) gains `priority: Option<bool>`, serde default + skip-if-none. `true` = ask the provider's priority/fast-token lane on every model call of the role; `false` and absent are ONE fact (no lane preference — the knob stays unset, the provider's default lane governs). No third state.
- `WorkerConfig` (`src/prompt/resolve/mod.rs`) and `Resolved` (`src/prompt/dispatch/resolved.rs`) carry it beside `effort`; `canonical::build_request` (`src/prompt/dispatch/canonical.rs`) sets `service_tier: priority.unwrap_or(false).then_some(brazen::ServiceTier::Priority)` (spelling per the published knob).
- Follow-the-tip (bl-403b) makes it switch mid-work at the next step boundary for free, same as effort — that IS the seat checkbox's semantics.
- Docs: extend the ARCH §4.3 Model-bullet paragraph bl-acba added (effort's sentence is the template) and the §4.4 Invocation note; `make schemas` regenerates `schemas/providers.json`.
- Tests at the 100% floor: parse (true/absent/junk declines), resolve-to-Resolved flow (copy `the_role_effort_follows_the_tip_to_the_resolved_shape`), build_request both arms.

**Vocabulary.** The config's word is **priority** — a checkbox fact. "service_tier"/"flex"/"batch" are provider mechanics that never enter `providers.yaml`; if a cost-saving lane is ever wanted it widens this field's type, not the checkbox upstream.

Gates: tests (100%, all pass), docs, alignment (ARCH/PRINCIPLES/TAXONOMY).