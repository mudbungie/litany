+++
title = "0.0.9 release prep: promote the changelog [Unreleased] section to [0.0.9]"
created = 1788581695
updated = 1788581697
claimant = "Animations-L"
priority = 1
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Run `make promote-changelog VERSION=0.0.9` in the claimed worktree and deliver it to main, mirroring the 0.0.6 and 0.0.7 preps. The [Unreleased] section carries the release's bullets; this ball only stamps it as [0.0.9].

Since bl-6364 this landing IS the release act: `release-plz.yml`'s merge-release-pr job holds the open release PR (#19, which proposes 0.0.9) until CHANGELOG.md at the gated commit names the version it proposes, and merges it on the next push that satisfies that. So no hand touches the pull request — the run this delivery triggers merges it and dispatches the workflow that publishes 0.0.9. It is also the proof of bl-6364's merge arm: the hold arm is already on the record (run 33918391492 printed the hold naming this exact command).