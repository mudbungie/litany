+++
title = "run_exchange resolves once per exchange: a workflow mark written mid-exchange waits for the next advance-driven step — collapse onto per-step consultation"
created = 1788236069
updated = 1788236069
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
The in-process root loop (dispatch::run_exchange) resolves WorkerConfig once and reuses it for every step of the exchange, so a workflow mark (bl-f928) written mid-exchange takes effect only from the next advance-driven step — the same latency retarget already has there. The §6 shipped-state note already records the prompt→advance collapse as an open simplification; either collapse prompt onto spawn+deposit+first-hop, or re-consult the workflow source at each loop boundary.