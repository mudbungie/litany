+++
title = "run_exchange resolves once per exchange: a workflow mark written mid-exchange waits for the next advance-driven step — collapse onto per-step consultation"
created = 1788236069
updated = 1788243981
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
SUPERSEDED IN SCOPE by the 2026-09-01 ruling (bl-403b): the in-process root loop (dispatch::run_exchange) resolves WorkerConfig once per exchange, so for a fresh root's FIRST exchange neither a workflow mark (bl-f928) nor the followed config tip (bl-403b) takes effect until the next advance-driven step. Every later exchange is advance-driven and per-hop. Fix: re-resolve at each loop boundary in run_exchange, or collapse prompt onto spawn+deposit+first-hop (the §6 shipped-state open simplification). This is now a ruling-conformance gap, not a convenience: 'changeable at any time, on any turn' includes mid-first-exchange.