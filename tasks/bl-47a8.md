+++
title = "tests: the leak self-test proves both new alternatives, both directions"
created = 1788146885
updated = 1788146885
parent = "bl-1408"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Gate subtask for bl-1408. `scripts/leak-scan.sh --self-test` must pass with the two new fixture lines flagged BY `session-artifact`, `clean.txt` and `clean-paths.txt` must stay unflagged, the whole-tree scan must be clean, and `make check` must pass at 100% coverage with `tests/changelog_completeness.rs` satisfied.