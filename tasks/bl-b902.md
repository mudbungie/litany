+++
title = "context maintenance is queued against the inevitable cache miss: a prefix edit (tool unload, description re-cut, loaded-set pruning) lands only at a boundary that already invalidates the prefix"
created = 1788752175
updated = 1788752175
priority = 3
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Ported from yog bl-b6f9 (the design note lives there; litany's assembly is the only place that knows when a miss is inevitable). Goal: each step's wire request shares the longest byte-identical prefix with the previous step's so provider prompt caching pays. (1) classify every between-step prefix edit as prefix-stable / deferrable / unavoidable; (2) implement the deferral with one invariant in ARCH §5.5 — a prefix edit is applied only at a boundary that already invalidates the prefix — and a test that assembles N consecutive steps and asserts the shared prefix length is monotone except at declared boundaries; (3) place cache breakpoints where the provider dialect wants them if brazen exposes that, else file the brazen ball and stop at the litany half. Amend ARCH §5 and DESIGN_CONTEXT_ECONOMY. Evidence: round-2 comparator — cache_read ~13k on every claude step after the first, 0 on gpt steps 3-4.