+++
title = "the brazen pin lands one release behind: 0.0.11 and 0.0.12 published while bl-3fe6 was in flight, and the shapes column is a fact a host selecting a row wants"
created = 1788581053
updated = 1788581054
claimant = "Animations-I"
priority = 2
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
bl-3fe6 pinned brazen =0.0.10 for the input_total_tokens counter and the ollama num_ctx fold. Two brazen releases landed while that ball was in the gate, so the pin is behind before litany's own release carries it.

0.0.11 is bl-9d21, an auto-merge job for brazen's release PR: repository infrastructure, no crate change litany can see. 0.0.12 is bl-5053, which publishes a per-row SHAPE capability on the read surface: Protocol::shapes() answers tools and multi_turn per dialect, bz --list-providers gains a shapes column (tools,multi_turn on every HTTP row, - on claude_code), and the declaration is proved against each dialect's own encode. That is a fact a host selecting a row for a tool-bearing role wants BEFORE it calls, rather than at encode time on the first call.

litany links brazen for the canonical types and drives the bz binary; neither half breaks on an additive read-surface column, so this is a pin bump and its proof. Pin the NEWEST published brazen at the moment of the edit (crates.io is the check, Cargo.toml the one home, README restates it and is gate-checked against it), refresh the lockfile, and note in the pin comment what the two releases carry. Gates: tests 100 percent, docs, alignment.